{
  "name": "hivesigner-ui-e2e",
  "private": true,
  "description": "Contract end-to-end suite for hivesigner-ui: the routes, headers and flows third-party apps depend on, run against a BASE_URL (staging by default).",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0"
  }
}
