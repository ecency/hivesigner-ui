{
  "name": "hivesigner-ui-e2e",
  "private": true,
  "description": "Parity end-to-end suite for hivesigner-ui. Runs against a BASE_URL (staging by default) and, once the React rewrite ships, against that build unchanged.",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0"
  }
}
